<h1>Stocks</h1>

<table class="table table-hover">
  <thead>
  <tr>
    <th width="50%">Company Name</th>
    <th width="50%">Ticker Symbol</th>
  </tr>
  </thead>
  <tbody>
  <% @stocks.each do |stock| %>
    <tr onclick="document.location = '<%= stock_path(stock) %>'">
      <td><%= stock.name %></td>
      <td><%= stock.ticker_symbol %></td>
    </tr>
  <% end %>
  </tbody>
</table>